Smart Home Dashboard - Flutter (Assignment 3)

How to run:
1. Ensure Flutter SDK is installed.
2. Place this folder inside your Flutter projects directory.
3. Run `flutter pub get`.
4. Replace the placeholder asset `assets/profile.jpg` with a real image or update the code.
5. Run on an emulator or device: `flutter run`.
